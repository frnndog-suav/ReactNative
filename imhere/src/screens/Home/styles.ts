import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    color: "#fdfcfe",
    fontSize: 24,
    fontWeight: "bold",
    marginTop: 48,
  },
  eventName: {
    color: "#6b6b6b",
    fontSize: 16,
  },
});
